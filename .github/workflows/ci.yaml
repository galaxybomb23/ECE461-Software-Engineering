name: CI

on:
  push:
    branches:
      - main
      - 30-setup-ci
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x' # Or whatever version you are using

      - name: Setup .env file and Install Dependencies
        run: |
          echo "API_TOKEN=${{ secrets.API_TOKEN }}" >> .env
          ./run install

      - name: Run tests
        run: ./run test

      - name: Clean up .env file
        run: rm .env